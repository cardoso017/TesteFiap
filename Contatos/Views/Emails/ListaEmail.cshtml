@model Contatos.Models.EntradaContrato;

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.emails.Email)
            </th>
     
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.ListarEmails)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>
              
                <td>
                    <a asp-action="Edit" asp-route-id="@item.IdEmail">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.IdEmail">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.IdEmail">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
